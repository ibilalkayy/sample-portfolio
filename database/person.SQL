CREATE TABLE IF NOT EXISTS Person (
    id INT PRIMARY KEY AUTO_INCREMENT,
    names VARCHAR(255) NOT NULL,
    emails VARCHAR(255) NOT NULL UNIQUE,
    subjects VARCHAR(255) NOT NULL,
    bodies VARCHAR(2000) NOT NULL
);